# ============================================================
# WV State Compliance Rules
# ------------------------------------------------------------
# This file contains ONLY WV's rules.
# Currently no rules are loaded for this state.
# ============================================================

STATE_RULES = {
    "compliance": {
        "state": "WV",
        "rulesChecked": [],
        "overallComplianceStatus": "PENDING_RULES",
        "notes": "No state-specific compliance rules loaded for WV.",
        "version": "0.1"
    }
}
