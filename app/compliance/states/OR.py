# ============================================================
# OR State Compliance Rules
# ------------------------------------------------------------
# This file contains ONLY OR's rules.
# Currently no rules are loaded for this state.
# ============================================================

STATE_RULES = {
    "compliance": {
        "state": "OR",
        "rulesChecked": [],
        "overallComplianceStatus": "PENDING_RULES",
        "notes": "No state-specific compliance rules loaded for OR.",
        "version": "0.1"
    }
}
