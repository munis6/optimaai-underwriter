# ============================================================
# CO State Compliance Rules
# ------------------------------------------------------------
# This file contains ONLY CO's rules.
# Currently no rules are loaded for this state.
# ============================================================

STATE_RULES = {
    "compliance": {
        "state": "CO",
        "rulesChecked": [],
        "overallComplianceStatus": "PENDING_RULES",
        "notes": "No state-specific compliance rules loaded for CO.",
        "version": "0.1"
    }
}
