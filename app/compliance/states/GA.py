# ============================================================
# GA State Compliance Rules
# ------------------------------------------------------------
# This file contains ONLY GA's rules.
# Currently no rules are loaded for this state.
# ============================================================

STATE_RULES = {
    "compliance": {
        "state": "GA",
        "rulesChecked": [],
        "overallComplianceStatus": "PENDING_RULES",
        "notes": "No state-specific compliance rules loaded for GA.",
        "version": "0.1"
    }
}
